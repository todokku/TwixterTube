import {
  UPDATE_CURRENT_LIKE,
  RECEIVE_LIKE,
  REMOVE_LIKE
} from "../actions/like_actions";

export default (state = {}, action) => {
  Object.freeze(state);
  let newState = {};
  //   switch (action.type) {
  //     case UPDATE_CURRENT_LIKE:
  //         return action.
  //   }
};
